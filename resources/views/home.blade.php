 {{-- <x-layout>

    <div class="container mx-auto py-8">
        <h1 class="text-3xl font-semibold text-center mb-6">Welcome  {{session('response')['user']->name}} to Our Airline Management System</h1>
        <div class="flex justify-center">
            <a href="{{ 'api/passengers' }}"
                class="bg-blue-500 hover:bg-blue-700 text-white font-bold py-2 px-4 rounded mr-4">View Passengers</a>
            <a href="{{ '/flights/show' }}"
                class="bg-green-500 hover:bg-green-700 text-white font-bold py-2 px-4 rounded mr-4">View Flights</a>
            <a href="{{ 'users/show' }}"
                class="bg-yellow-500 hover:bg-yellow-700 text-white font-bold py-2 px-4 rounded">View Users</a>
            <a href="{{ 'api/users/1' }}"
                class="bg-yellow-500 hover:bg-yellow-700 text-white font-bold py-2 px-4 rounded">Logout</a>
        </div>
    </div>
</x-layout>  --}}
